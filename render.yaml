services:
  - type: web
    name: ai-kitchen
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npx serve -s . -p $PORT
    envVars:
      - key: NODE_ENV
        value: production