services:
  - type: web
    name: ai-kitchen
    env: node
    plan: free
    buildCommand: npm install
    startCommand: serve -s . --host 0.0.0.0 --port $PORT
    envVars:
      - key: NODE_ENV
        value: production